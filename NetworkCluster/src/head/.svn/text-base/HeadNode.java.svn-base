package head;

import java.util.Set;

public class HeadNode extends Thread{
	
	ClientManager clientManager;
	
	public HeadNode(ClientManager clientManager){
		this.clientManager = clientManager;
	}
	
	public void run(){
		//break up the work and give each server a portion of it
		int numberOfClients = 2;
		Set<ClientConnection> clients = (Set<ClientConnection>) clientManager.getClients().values();
		
		for(ClientConnection c : clients){
			
		}
	}

}
